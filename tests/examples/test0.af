# Tests deep references
# Tests erasure of substitutions
# Tests unnamed map members
# Tests numeric arithmetic

48 / 32 + (100 / 70)^(6/3) = ``,
x  : { 
	2 / 3 - 5/7 = ``,
	-  1 + 2 * - 3.1 = ``,
	67 ^ (2 / 3) * 67^-1 - pi = ``,
	2 ^ 1.1 ^ 1.01 ^ (23 % num.e) = ``,
	34 // 2.45 % e / 3 * 15 % num.pi = ``,
	round (gold ^ 20) = `Old Stuff`,
	m: ceil (floor y.z.r / 3) = ``,
	abs m * signum (1 - m ^ 2) = ``,
	sin (atan2 67 32) = ``, 
	
	factorial 10 / 91 = ``,
	num.choose (sqrt 17) 4 = ``,
	lcm 21 (gcd 23 69) = ``,
y: {
 z
: 
{r
: 78 - 45 / 5 ^
2}}},

answer: 2 * x.y.z.r = `bjnkkm`

