# Test Builtin Constants and Functions

v : 5 * pi * log(5,2)
sqrt(cos(v - 2*e) + 2) = 

# Try to apply log
sc:abs(sin(word)
	+ cos(pi/v))

log(sc, 2) - log(
sin(1 / 2)  ,
word
)=

word : ln(e + pi)

notdefined = # Should produce no expr error

