# Test vector operations

v1: V[1, 2.5, -3 / 13],
v2: lin.V[0, -7.234, 57 / 7],
v3: V[12, 9.35, 23 / 27],

# Linear operations
-v1 + v2 - v3 = ``,
3.4 * v1 + v2 / 10 = ``,
v1 - v3 % (5 / 4) = ``,
(v3 // 1.2) + (v1 // 0.8) = ``,

# Dot product
v1 * (v2 + v3) = ``,
V[3/4, 2/3, -8/9] * V[1, -2, 10] = ``,
v1 - (v1 * v2) * v2 = ``,

# Vector methods
v1.mag = ``, v2.mag2 = ``,
v3.dims = ``,
(v2 + v1).comps = ``,

# Dimension mismatch errors
other: lin.V[1, 2, 0, 3],
other + v1 = ``,
v2 * other = ``,

# Vector Unimplemented operations
v1 ^ 2 = ``,
1.1 ^ v2 = ``,
3 / v3 = ``,
5 % v1 = ``,

